import styled from "styled-components";

const WeatherContainer = styled.div`
  display: flex;
  flex-wrap: wrap;
  align-items: center;

  figure {
    margin: 0;
  }
`

export {
  WeatherContainer
}